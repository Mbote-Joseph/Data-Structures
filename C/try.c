#include<stdio.h>

typedef struct node
{
    int number;
    struct node *next;
}
node;

// node *list= NULL;
// node *n= malloc(sizeof(node));

// if(n != NULL)
// {
//     (*n).number=1;
// }


int main(){


    return 0;
}